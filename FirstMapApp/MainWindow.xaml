<Window x:Class="FirstMapApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mahApp="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro" 
        xmlns:maptor="clr-namespace:IRI.Maptor.Jab.Controls.View;assembly=IRI.Maptor.Jab.Controls"  
        Loaded="Window_Loaded" Title="Sample Maptor App" Height="700" Width="900" FlowDirection="{Binding CurrentFlowDirection}">
    <Window.Resources>
        <Style TargetType="{x:Type TabItem}" BasedOn="{StaticResource {x:Type TabItem}}">
            <Setter Property="mahApp:HeaderedControlHelper.HeaderFontSize" Value="12" />
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" MinWidth="200"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        
        <maptor:LanguageCombo/>
        
        <StackPanel Grid.Column="1">
            <Border BorderBrush="Black" BorderThickness="1" Margin="2">
                <StackPanel Orientation="Horizontal" >
                    <Button Margin="4" Style="{StaticResource flatButtonPrimary}" Content="{Binding ClearText}" Command="{Binding ClearAllCommand}"/>
                    <Button Margin="4" Style="{StaticResource flatButtonPrimary}" Content="{Binding FullExtentText}" Command="{Binding FullExtentCommand}"/>
                    <Button Margin="4" Style="{StaticResource flatButtonPrimary}" Content="{Binding AddShapefileText}" Command="{Binding AddShapefileCommand}"/>
                    <Button Margin="4" Style="{StaticResource flatButtonPrimary}" Content="{Binding MeasureLengthText}" Command="{Binding MeasureLengthCommand}"/>
                    <Button Margin="4" Style="{StaticResource flatButtonPrimary}" Content="{Binding MeasureAreaText}" Command="{Binding MeasureAreaCommand}"/>
                    <Button Margin="4" Style="{StaticResource flatButtonPrimary}" Content="{Binding DrawPointText}" Command="{Binding DrawPointCommand}"/>
                    <Button Margin="4" Style="{StaticResource flatButtonPrimary}" Content="{Binding DrawPolylineText}" Command="{Binding DrawPolylineCommand}"/>
                    <Button Margin="4" Style="{StaticResource flatButtonPrimary}" Content="{Binding DrawPolygonText}" Command="{Binding DrawPolygonCommand}"/>
                    <Button Margin="4" Style="{StaticResource flatButtonPrimary}" Content="{Binding GoToText}" Command="{Binding GoToCommand}"/>
                </StackPanel>
            </Border> 
        </StackPanel>

        <maptor:MapViewer Grid.Row="1" Grid.Column="1" x:Name="map" BorderBrush="Black" BorderThickness="1"/>

        <maptor:MapInfoView Grid.Row="1" Grid.Column="1" DataContext="{Binding }"/>

        <maptor:CoordinatePanelView Grid.Column="1" Grid.Row="1" Margin="10" HorizontalAlignment="Right" VerticalAlignment="Bottom"
                                    DataContext="{Binding CoordinatePanel}" Position="{Binding CurrentPoint, ElementName=map}"/>

        <StackPanel Grid.Row="1">
            <Expander DockPanel.Dock="Top" IsExpanded="True" Margin="0,1,0,0" Cursor="Hand" Header="{Binding BaseMapsText}">
                <ComboBox ItemsSource="{Binding MapProviders}" SelectedItem="{Binding SelectedMapProvider, Mode=TwoWay}" DisplayMemberPath="Title"/>
            </Expander>
            <Expander DockPanel.Dock="Top" IsExpanded="True" Margin="0,1,0,0" Cursor="Hand" Header="{Binding DrawingsText}">
                <maptor:MapDrawingLegendView MinHeight="90"/>
            </Expander>
            <Expander DockPanel.Dock="Top" IsExpanded="True" Margin="0,1,0,0" Cursor="Hand" Header="{Binding LayersText}">
                <maptor:MapLegendWithOptionsView EnableFilterMode="True" DataContext="{Binding Layers}" GroupName="B"/>
            </Expander>
        </StackPanel>
    </Grid>
</Window>
